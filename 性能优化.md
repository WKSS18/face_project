#### 1 强制刷新 & 手动刷新
```
function useForceUpdate() {
  const [, setTick] = useState(0);
  const forceUpdate = React.useCallback(() => {
    setTick(tick => tick + 1);
  }, []);
  return forceUpdate;
}

  const handleKeyUpdate = () => {
    setKey(prev => prev + 1); // key作为子组件的属性
  };
```


# 移动端React应用白屏时间优化指南

## 1. 性能指标说明

### FP (First Paint)
- 首次绘制时间，浏览器第一次渲染任何内容的时间点

### FCP (First Contentful Paint)
- 首次内容绘制时间，浏览器第一次渲染文本、图片等内容的时间点

### LCP (Largest Contentful Paint)
- 最大内容绘制时间，页面最大内容元素渲染完成的时间

### CLS (Cumulative Layout Shift)
- 累积布局偏移，衡量页面加载过程中布局稳定的程度

## 2. 计算方法

### 传统白屏时间计算
```javascript
const whitescreenTime = performance.timing.responseStart - performance.timing.navigationStart;
```

### 现代API计算
```javascript
// 使用PerformanceObserver监控FP/FCP
const observer = new PerformanceObserver((list) => {
  list.getEntries().forEach((entry) => {
    if (entry.name === 'first-contentful-paint') {
      console.log('FCP:', entry.startTime);
    }
  });
});
observer.observe({ entryTypes: ['paint'] });
```

## 3. 优化措施

### 3.1 资源加载优化
- **代码分割**: 使用React.lazy和Suspense按需加载组件
- **预加载关键资源**: 使用<link rel="preload">预加载首屏必需资源
- **资源压缩**: 压缩图片、CSS、JavaScript文件

### 3.2 网络优化
- **CDN部署**: 将静态资源部署到CDN节点
- **HTTP/2**: 启用HTTP/2协议提高并发能力
- **Service Worker**: 使用Service Worker缓存关键资源

### 3.3 渲染优化
- **骨架屏**: 使用骨架屏替代白屏等待
- **SSR/SSG**: 服务端渲染或静态站点生成
- **虚拟滚动**: 长列表使用虚拟滚动技术

### 3.4 图片优化
- **懒加载**: 非首屏图片使用懒加载
- **响应式图片**: 使用srcset和sizes属性
- **现代图片格式**: 使用WebP等现代图片格式

## 4. React特定优化

### 4.1 组件优化
```javascript
// 使用React.memo避免不必要的重渲染
const OptimizedComponent = React.memo(({ data }) => {
  return <div>{data.title}</div>;
});

// 使用useMemo缓存计算结果
const expensiveValue = useMemo(() => {
  return computeExpensiveValue(data);
}, [data]);

// 使用useCallback缓存函数引用
const handleClick = useCallback(() => {
  doSomething();
}, []);
```

### 4.2 路由优化
```javascript
// 路由级别的代码分割
const HomePage = React.lazy(() => import('./HomePage'));
const AboutPage = React.lazy(() => import('./AboutPage'));

function App() {
  return (
    <Suspense fallback={<div>加载中...</div>}>
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/about" element={<AboutPage />} />
      </Routes>
    </Suspense>
  );
}
```

## 5. 移动端特殊优化

### 5.1 触摸优化
- **触摸目标大小**: 确保触摸目标至少44px
- **消除300ms延迟**: 使用touch-action: manipulation
- **手势优化**: 实现流畅的手势交互

### 5.2 网络状况适应
- **离线支持**: 使用Service Worker实现离线功能
- **慢网优化**: 针对慢网环境提供简化版体验
- **连接感知**: 根据网络状况调整资源加载策略

### 5.3 设备适配
- **响应式设计**: 使用媒体查询适配不同屏幕尺寸
- **Retina优化**: 为高清屏幕提供高质量图片
- **方向锁定**: 处理屏幕旋转带来的布局变化

## 6. 监控与调试

### 6.1 性能监控
- **真实用户监控(RUM)**: 收集真实用户的性能数据
- **实验室测试**: 使用Lighthouse等工具定期检测
- **异常报警**: 设置性能阈值，超出时及时报警

### 6.2 调试工具
- **Chrome DevTools**: 使用Performance面板分析性能
- **React DevTools**: 分析React组件渲染性能
- **Webpack Bundle Analyzer**: 分析打包文件大小

## 7. 最佳实践总结

1. **优先级排序**: 首屏内容优先加载
2. **渐进增强**: 基础功能先呈现，增强功能后续加载
3. **用户体验**: 白屏期间提供友好提示
4. **持续优化**: 定期监控性能指标，持续迭代优化
